version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static baby shower site - no build step required"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
