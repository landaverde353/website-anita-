version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static baby shower site - no build step required"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
